

node升级：

```
node -v    //查看版本号

sudo npm cache clean -f //清除nodejs的cache

sudo npm i -g n  //使用npm安装n模块

npm view node versions  //查看node的所有版本

sudo n stable //将node升级到稳定版本

sudo n latest //将node升级到最新版本

sudo n xx.xx  //将node升级到指定版本

```


